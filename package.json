{
  "name": "bcv-scrapper",
  "version": "1.0.0",
  "description": "Scrapper for Banco Centrar de Venezuela. This will only get the exchange currency dollar-bolivar and will not modify it.",
  "main": "scrape.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.25.0",
    "cheerio": "^1.0.0-rc.10",
    "dotenv": "^14.2.0",
    "request": "^2.88.2"
  }
}
